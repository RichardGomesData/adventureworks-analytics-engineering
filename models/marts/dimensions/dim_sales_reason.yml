version: 2

models:
  - name: dim_sales_reason

    columns:

      - name: pk_sales_reason
        tests:
          - unique
          - not_null

      - name: nome_motivo_venda

      - name: tipo_motivo_venda