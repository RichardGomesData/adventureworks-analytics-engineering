version: 2

models:
  - name: stg_person_business_entity_address
    description: Bridge table linking business entities and addresses.

    tests:
      - unique:
          column_name: "(fk_business_entity, fk_address, fk_address_type)"

    columns:

      - name: fk_business_entity
        description: Foreign key referencing the business entity.
        tests:
          - not_null

      - name: fk_address
        description: Foreign key referencing the address.
        tests:
          - not_null

      - name: fk_address_type
        description: Foreign key referencing the address type.
        tests:
          - not_null

      - name: ts_modificacao
        description: Timestamp of the last modification.